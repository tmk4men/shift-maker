{
  "name": "shift-maker",
  "version": "1.0.0",
  "description": "ルールブック方式のシフト自動作成アプリ（AI不使用・ローカル完結）",
  "private": true,
  "scripts": {
    "test": "node tests/test.js && node tests/ui-smoke.js && node tests/image.js && node tests/contrast.js",
    "build:www": "node scripts/build-www.mjs",
    "sync": "npm run build:www && npx cap sync ios",
    "open:ios": "npx cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^8.0.0",
    "@capacitor/ios": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0"
  }
}
